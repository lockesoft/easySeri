RewriteEngine On
RewriteBase /easyseri/

# Si el archivo o carpeta existe, se sirve normal
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Todo lo demás pasa por index.php
RewriteRule ^ index.php [L,QSA]